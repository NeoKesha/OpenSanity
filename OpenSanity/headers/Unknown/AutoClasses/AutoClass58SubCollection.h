#pragma once

#include "headers/OpenSanityGlobal.h"

class AutoClass58SubCollection {
	public:
		RendererText * * array;
		int count;
		int capacity;
		int step;

		void* Add(RendererText* element);

};
