#include "headers/Known/Collections/Matrix4Array.h"

#include "headers/Known/Math/Matrix4.h"
void Matrix4Array::Construct(int cnt) {
	Logging::LogUnimplemented(__FUNCSIG__);
	/*
	Matrix4Array * __thiscall Matrix4Array::Construct(Matrix4Array *this,int cnt){
		Matrix4 **ppMVar1;
		this->cnt = cnt;
		if (cnt == 0) {
		this->array = (Matrix4 **)0x0;
		return this;
		}
		ppMVar1 = (Matrix4 **)VirtualPool::AllocateMemory(cnt << 2);
		this->array = ppMVar1;
		return this;
		}
		
	*/
	return;
}

